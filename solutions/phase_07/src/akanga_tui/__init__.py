"""akanga_tui — the Phase 5 Textual terminal interface.

Single-process: imports ``akanga_core`` directly instead of speaking to
the Phase 6 REST API, so navigation stays latency-free and no server
needs to be running. The application class is ``akanga_tui.app.AkangaTUI``.
"""
