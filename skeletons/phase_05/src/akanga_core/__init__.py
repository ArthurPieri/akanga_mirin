# akanga_core — Phase 05 placeholder
# All prior-phase modules (parser, db, indexer, links, eventbus, watcher)
# are assumed complete and available on PYTHONPATH.
# This directory exists so the TUI can import akanga_core directly.
