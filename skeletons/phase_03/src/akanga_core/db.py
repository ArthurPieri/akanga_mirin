# Copy your Phase 02 solution here, or run with AKANGA_SRC pointing to your Phase 02 src/ directory.
# This file is intentionally left as a reference marker — the skeleton runner picks up
# your actual implementation from AKANGA_SRC.
#
# Phase 03's build_ego_graph requires the full Phase 02 GraphDatabase, in particular:
#   get_node, get_neighbors, get_backlinks,
#   get_edges_from(node_id) -> list[(Node, relation, relation_id)],
#   get_edges_to(node_id)   -> list[(Node, relation, relation_id)]
